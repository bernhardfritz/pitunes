mutation UpdateTrackMutation($id: Int!, $trackInput: TrackInput!) {
  updateTrack(id: $id, trackInput: $trackInput) {
    id
    uuid
    name
    duration
    album {
      id
      name
    }
    artist {
      id
      name
    }
    genre {
      id
      name
    }
    trackNumber
  }
}
