mutation UpdatePlaylistTrackMutation($id: ID!, $input: PlaylistTrackOrderInput!) {
  updatePlaylistTrack(id: $id, input: $input) {
    id
    name
    tracks {
      id
      name
      duration
      album {
        id
        name
      }
      artist {
        id
        name
      }
      genre {
        id
        name
      }
      trackNumber
    }
  }
}
