query ArtistTracksQuery($id: Int!) {
  artist(id: $id) {
    id
    name
    tracks {
      id
      uuid
      name
      duration
      album {
        id
        name
      }
      artist {
        id
        name
      }
      genre {
        id
        name
      }
      trackNumber
    }
  }
}
