query TrackQuery($id: Int!) {
  track(id: $id) {
    id
    uuid
    name
    duration
    album {
      id
      name
    }
    artist {
      id
      name
    }
    genre {
      id
      name
    }
    trackNumber
  }
}
