mutation UpdatePlaylistTrackMutation($id: Int!, $playlistTrackOrderInput: PlaylistTrackOrderInput!) {
  updatePlaylistTrack(id: $id, playlistTrackOrderInput: $playlistTrackOrderInput) {
    id
    name
    tracks {
      id
      name
    }
  }
}
