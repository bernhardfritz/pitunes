query PlaylistTracksQuery($id: Int!) {
  playlist(id: $id) {
    id
    name
    tracks {
      id
      name
      duration
      album {
        id
        name
      }
      artist {
        id
        name
      }
      genre {
        id
        name
      }
    }
  }
}
